/**
 * supabaseClient — re-export de src/lib/supabase.js
 * Mantido para compatibilidade com imports existentes.
 */
export { sbFetch, sbFetch as default, SUPABASE_ENABLED, SB_URL, SB_KEY } from '../lib/supabase';
